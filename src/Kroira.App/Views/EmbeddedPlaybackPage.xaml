<Page
    x:Class="Kroira.App.Views.EmbeddedPlaybackPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Black">

    <Grid>
        <MediaPlayerElement x:Name="PlayerElement" 
                            AreTransportControlsEnabled="True" 
                            AutoPlay="True" 
                            HorizontalAlignment="Stretch" 
                            VerticalAlignment="Stretch"
                            DoubleTapped="DoubleTapOverlay_DoubleTapped" />

        <!-- Overlay Back Button — hidden during fullscreen for clean immersion -->
        <Button x:Name="BackButton" Content="← Back" Click="Back_Click" 
                VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20" 
                Background="#88000000" Foreground="White" Padding="12,8" CornerRadius="4" />
    </Grid>
</Page>
