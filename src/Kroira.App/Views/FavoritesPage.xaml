<Page
    x:Class="Kroira.App.Views.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource KroiraPageBackgroundBrush}">

    <Grid Padding="34,30,36,34" RowSpacing="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="6">
                <TextBlock Text="Favorites" Style="{StaticResource KroiraPageTitleStyle}" />
                <TextBlock
                    Text="A saved hub for the live channels you return to most."
                    Style="{StaticResource KroiraPageSubtitleStyle}"
                    MaxWidth="680" />
            </StackPanel>

            <Border
                Grid.Column="1"
                Background="{StaticResource KroiraSurfaceSoftBrush}"
                CornerRadius="8"
                Padding="12,7"
                VerticalAlignment="Center"
                Visibility="{x:Bind ViewModel.ContentVisibility, Mode=OneWay}">
                <TextBlock
                    Text="{x:Bind ViewModel.FavoriteCountText, Mode=OneWay}"
                    Foreground="{StaticResource KroiraAccentBrush}"
                    FontSize="12"
                    FontWeight="SemiBold" />
            </Border>
        </Grid>

        <Grid
            Grid.Row="1"
            Visibility="{x:Bind ViewModel.EmptyVisibility, Mode=OneWay}"
            ColumnSpacing="22"
            MaxWidth="1080"
            HorizontalAlignment="Left"
            VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.35*" />
                <ColumnDefinition Width="0.9*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource KroiraPanelStyle}" Padding="28">
                <Grid RowSpacing="22">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Spacing="14">
                        <Border
                            Width="58"
                            Height="58"
                            CornerRadius="14"
                            Background="{StaticResource KroiraSurfaceElevatedBrush}"
                            HorizontalAlignment="Left">
                            <FontIcon Glyph="&#xE734;" Foreground="{StaticResource KroiraGoldBrush}" FontSize="24" />
                        </Border>
                        <TextBlock Text="No saved channels yet" Style="{StaticResource KroiraSectionTitleStyle}" />
                        <TextBlock
                            Text="Mark channels as favorites and they will appear here as a focused live lineup with quick playback access."
                            Style="{StaticResource KroiraPageSubtitleStyle}"
                            MaxWidth="560" />
                    </StackPanel>

                    <Grid Grid.Row="1" ColumnSpacing="14" VerticalAlignment="Bottom">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Background="{StaticResource KroiraSurfaceSoftBrush}"
                                BorderBrush="{StaticResource KroiraGhostBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource KroiraRadiusCard}"
                                Padding="16">
                            <StackPanel Spacing="8">
                                <TextBlock Text="How to save" Style="{StaticResource KroiraLabelStyle}" />
                                <TextBlock Text="Open Live TV" Style="{StaticResource KroiraTitleStyle}" />
                                <TextBlock Text="Use the star on any channel tile to add it here." Style="{StaticResource KroiraBodyStyle}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1"
                                Background="{StaticResource KroiraSurfaceSoftBrush}"
                                BorderBrush="{StaticResource KroiraGhostBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource KroiraRadiusCard}"
                                Padding="16">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Saved scope" Style="{StaticResource KroiraLabelStyle}" />
                                <TextBlock Text="Live channels" Style="{StaticResource KroiraTitleStyle}" />
                                <TextBlock Text="Favorites are kept as a clean launch surface for channel playback." Style="{StaticResource KroiraBodyStyle}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1" Spacing="14">
                <Border Style="{StaticResource KroiraPanelStyle}" Padding="20">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Fast access" Style="{StaticResource KroiraSectionTitleStyle}" />
                        <TextBlock Text="Your saved channels will appear as rows with logos, live status, and a remove action." Style="{StaticResource KroiraBodyStyle}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KroiraPanelStyle}" Padding="20">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Playback behavior" Style="{StaticResource KroiraSectionTitleStyle}" />
                        <TextBlock Text="Selecting a favorite keeps the same playback launch path as Live TV." Style="{StaticResource KroiraBodyStyle}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <Grid
            Grid.Row="1"
            Visibility="{x:Bind ViewModel.ContentVisibility, Mode=OneWay}"
            ColumnSpacing="22"
            MaxWidth="1160"
            HorizontalAlignment="Left"
            VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource KroiraPanelStyle}" Padding="0">
                <Grid RowSpacing="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Padding="18,16,18,12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="3">
                            <TextBlock Text="Saved channels" Style="{StaticResource KroiraSectionTitleStyle}" />
                            <TextBlock
                                Text="Select a row to start playback. Remove only clears it from this saved list."
                                Style="{StaticResource KroiraBodyStyle}" />
                        </StackPanel>
                        <TextBlock
                            Grid.Column="1"
                            Text="LIVE"
                            Foreground="{StaticResource KroiraTextTertiaryBrush}"
                            FontSize="11"
                            FontWeight="SemiBold"
                            CharacterSpacing="80"
                            VerticalAlignment="Center" />
                    </Grid>

                    <ListView
                        Grid.Row="1"
                        ItemsSource="{x:Bind ViewModel.FavoriteChannels, Mode=OneWay}"
                        SelectionMode="Single"
                        SelectionChanged="ChannelList_SelectionChanged"
                        ItemContainerStyle="{StaticResource KroiraMediaRowContainerStyle}"
                        Padding="10,0,10,10">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    Background="{StaticResource KroiraSurfaceSoftBrush}"
                                    CornerRadius="8"
                                    Padding="14,12">
                                    <Grid ColumnSpacing="14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="62" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <Border
                                            Width="54"
                                            Height="54"
                                            CornerRadius="8"
                                            Background="{StaticResource KroiraMediaGradientBrush}"
                                            VerticalAlignment="Center">
                                            <Grid>
                                                <FontIcon Glyph="&#xE714;" Foreground="{StaticResource KroiraAccentBrush}" FontSize="18" Opacity="0.55" />
                                                <Image Source="{Binding LogoUrl, Converter={StaticResource UrlToImageSourceConverter}}" Stretch="Uniform" Margin="7" />
                                            </Grid>
                                        </Border>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="5">
                                            <TextBlock
                                                Text="{Binding Name}"
                                                Style="{StaticResource KroiraTitleStyle}"
                                                MaxLines="2"
                                                TextTrimming="CharacterEllipsis" />
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <Border
                                                    Width="7"
                                                    Height="7"
                                                    CornerRadius="4"
                                                    Background="{StaticResource KroiraLiveDotBrush}"
                                                    VerticalAlignment="Center" />
                                                <TextBlock
                                                    Text="Favorite channel"
                                                    Foreground="{StaticResource KroiraTextSecondaryBrush}"
                                                    FontSize="12" />
                                            </StackPanel>
                                        </StackPanel>

                                        <Button
                                            Grid.Column="2"
                                            Content="Remove"
                                            Tag="{Binding Id}"
                                            Click="RemoveFavorite_Click"
                                            Style="{StaticResource KroiraDangerButtonStyle}"
                                            FontSize="12"
                                            VerticalAlignment="Center" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1" Spacing="14">
                <Border Style="{StaticResource KroiraPanelStyle}" Padding="18">
                    <StackPanel Spacing="12">
                        <Border
                            Height="5"
                            Width="72"
                            Background="{StaticResource KroiraGoldBrush}"
                            CornerRadius="3"
                            HorizontalAlignment="Left" />
                        <TextBlock Text="Built for quick return" Style="{StaticResource KroiraSectionTitleStyle}" />
                        <TextBlock
                            Text="Favorites are intentionally focused on channels in the current data model, so this page behaves like a reliable live lineup instead of a mixed placeholder."
                            Style="{StaticResource KroiraBodyStyle}" />
                    </StackPanel>
                </Border>

                <Border
                    Background="{StaticResource KroiraSurfaceSoftBrush}"
                    CornerRadius="10"
                    Padding="16">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Saved type" Style="{StaticResource KroiraLabelStyle}" />
                        <TextBlock Text="Channels" Style="{StaticResource KroiraTitleStyle}" />
                        <TextBlock
                            Text="Movie and series favorites can be added here later when the data path supports them safely."
                            Style="{StaticResource KroiraBodyStyle}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
